en-US:
  activerecord:
    errors:
      models:
        topup:
          attributes:
            external_id:
              blank: 'cannot be blank'
            phone_number:
              blank: 'cannot be empty'
            amount:
              blank: 'must be greater than 0 and not empty'
              not_a_number: 'must be a number'
      messages:
        record_invalid: 'Validation failed: %{errors}'
  errors:
    messages:
      external_id_not_blank: 'External_id cannot be blank'
      phone_number_not_blank: 'Phone number cannot be empty'
      amount_not_blank: 'Value must be greater than 0 and not empty'
      idempotency_conflict: 'Idempotency conflict'
      unexpected_error: 'Unexpected error: %{error}'
      taken: 'already exists'
